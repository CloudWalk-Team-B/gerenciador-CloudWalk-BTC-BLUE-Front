import styled, { css } from "styled-components";

export const AboutInfo = styled.div`
  ${({ theme }) => css`
    width: 100vw;
    color: white;
  `}
`;

export const InfoProject = styled.div``;
export const Info = styled.p`
  position: absolute;
  top: 145px;
  left: 30vw;
`;

export const ImageCapivara = styled.img`
  width: 100%;
  height: 90vh;
  display: inline-block;
  position: relative;
`;
